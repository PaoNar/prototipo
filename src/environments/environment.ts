// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: "AIzaSyAaV7gwTb3VfMNlPNZ-TgKwv8DivdInUfw",
    authDomain: "formulariopao.firebaseapp.com",
    databaseURL: "https://formulariopao.firebaseio.com",
    projectId: "formulariopao",
    storageBucket: "formulariopao.appspot.com",
    messagingSenderId: "201725382867",
    appId: "1:201725382867:web:8e1a61915dae9a6c"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
